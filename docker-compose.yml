version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: rjdferreira-web
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
